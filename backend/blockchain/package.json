{
  "name": "agritrust-contracts",
  "version": "1.0.0",
  "description": "Smart Contracts for AgriTrust Full Stack MVP",
  "scripts": {
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "ethers": "^6.8.1",
    "hardhat": "^2.19.1",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  }
}
